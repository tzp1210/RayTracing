#ifndef SCREEN_SIZE_H
#define SCREEN_SIZE_H

#include "Color.h"

namespace ScreenSize
{
	const int width = 600;
	const int height = 600;
}

namespace Constants
{
	const double infiniteDistance = 10000.0;
	const Color backgroundColor(0.0, 0.0, 0.0);
	const double epsilon = 0.02;
}

#endif
